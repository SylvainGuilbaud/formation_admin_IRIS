> **⚠️ Prerequisites**
> 
> Before running `docker-compose up -d`, you **must** first execute the `perm.sh` script to set the required file permissions:
> 
> ```bash
> ./perm.sh
> ```
> 
> This step is mandatory to ensure that the necessary permissions are correctly applied before starting the Docker containers.